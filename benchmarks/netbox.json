{
  "triage_version": 1,
  "fingerprint_version": "djaudit/v1",
  "target": "netbox",
  "repo": "https://github.com/netbox-community/netbox",
  "sha": "4877d1167f4e20c0507bc902ee4c708e20f92e36",
  "max_false_positive_rate": 0.1,
  "findings": [
    {
      "fingerprint": "c2ae47c4a80cb3c1",
      "rule_id": "DJA-003",
      "verdict": "accepted_risk",
      "file": "netbox/users/api/views.py",
      "line": 56,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-04",
      "note": "TokenProvisionView at users/api/views.py:56. The reading is exactly right: permission_classes = [] and the route is POST, so an anonymous caller can reach it. It is also deliberate, and its own docstring says so -- \"Non-authenticated REST API endpoint via which a user may create a Token\". This is the credential-exchange endpoint: the caller POSTs a username and password, TokenProvisionSerializer validates them, and a Token comes back. Every token-authenticated API needs exactly one door like this, and it cannot be behind the authentication it exists to grant. Recorded as accepted rather than a false positive because the finding's claim is true and worth a reviewer's five seconds -- an endpoint that opens itself to anonymous POST should have to justify itself once, and this one does. The credential check happens inside the serializer, which DJA-003 states plainly that it cannot see."
    },
    {
      "fingerprint": "745b70a3b644900f",
      "rule_id": "DJS-006",
      "verdict": "accepted_risk",
      "file": "netbox/netbox/settings.py",
      "line": 205,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-02",
      "note": "SECURE_SSL_REDIRECT at settings.py:205. NetBox exposes this as a first-class, documented configuration parameter -- it is written up in docs/configuration/security.md and was introduced deliberately in 3.4 (issue #11386) alongside the other two. The False in settings.py is the fallback for `getattr(configuration, ...)`, where `configuration` is the operator's configuration.py, a file that lives outside the repository we are scanning and is therefore invisible to static analysis by construction. There is nothing here for a NetBox developer to change: the value is the operator's to set. Recorded as accepted rather than a false positive because the statement the rule makes -- that the shipped default is off -- is true, and the finding is annotated environment-dependent for exactly this reason. NetBox additionally documents running behind nginx or Apache, which is the usual place for this redirect."
    },
    {
      "fingerprint": "2ddc67e5415029ff",
      "rule_id": "DJS-007",
      "verdict": "accepted_risk",
      "file": "netbox/netbox/settings.py",
      "line": 204,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-02",
      "note": "Correct, and quiet on purpose. NetBox exposes SECURE_HSTS_SECONDS at settings.py:204 as a documented operator knob defaulting to 0, alongside SECURE_HSTS_INCLUDE_SUBDOMAINS and SECURE_HSTS_PRELOAD, so enabling it is a deliberate operator decision. HSTS is the one security header whose absence from the Django settings is weak evidence: nginx, a load balancer and every CDN can send Strict-Transport-Security, it is extremely common to set it there, and none of that is visible from inside the repository. The rule reports this at low/tentative for exactly that reason, which is why it does not appear in a default run. Recorded as accepted rather than a false positive because the statement is true -- Django is not sending the header -- and because a project that assumes the proxy is doing it and is wrong has no way to find out except by being attacked."
    },
    {
      "fingerprint": "60882482a7c33223",
      "rule_id": "DJS-009",
      "verdict": "accepted_risk",
      "file": "netbox/netbox/settings.py",
      "line": 219,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-02",
      "note": "SESSION_COOKIE_SECURE at settings.py:219. NetBox exposes this as a first-class, documented configuration parameter -- it is written up in docs/configuration/security.md and was introduced deliberately in 3.4 (issue #11386) alongside the other two. The False in settings.py is the fallback for `getattr(configuration, ...)`, where `configuration` is the operator's configuration.py, a file that lives outside the repository we are scanning and is therefore invisible to static analysis by construction. There is nothing here for a NetBox developer to change: the value is the operator's to set. Recorded as accepted rather than a false positive because the statement the rule makes -- that the shipped default is off -- is true, and the finding is annotated environment-dependent for exactly this reason."
    },
    {
      "fingerprint": "a1ef84b11c682b77",
      "rule_id": "DJS-010",
      "verdict": "accepted_risk",
      "file": "netbox/netbox/settings.py",
      "line": 100,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-02",
      "note": "CSRF_COOKIE_SECURE at settings.py:100. NetBox exposes this as a first-class, documented configuration parameter -- it is written up in docs/configuration/security.md and was introduced deliberately in 3.4 (issue #11386) alongside the other two. The False in settings.py is the fallback for `getattr(configuration, ...)`, where `configuration` is the operator's configuration.py, a file that lives outside the repository we are scanning and is therefore invisible to static analysis by construction. There is nothing here for a NetBox developer to change: the value is the operator's to set. Recorded as accepted rather than a false positive because the statement the rule makes -- that the shipped default is off -- is true, and the finding is annotated environment-dependent for exactly this reason."
    },
    {
      "fingerprint": "47a5c85aea75a78f",
      "rule_id": "DJS-012",
      "verdict": "accepted_risk",
      "file": "netbox/netbox/settings.py",
      "line": 601,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-02",
      "note": "Correct, well-formed, and exactly what the rule is designed to say quietly rather than loudly. netbox/netbox/settings.py:601 is the textbook ('HTTP_X_FORWARDED_PROTO', 'https'), which is the right configuration for the nginx or Apache deployment NetBox documents, and the line immediately below it sets USE_X_FORWARDED_HOST = True for the same reason. There is no misspelling and no alternative NetBox could sensibly choose. What the finding records is the invariant this creates -- that every route to the application must pass through a proxy which overwrites the header -- and that invariant is a deployment property we cannot see from the repository, which is why the rule caps itself at low/tentative and stays out of a default run. Accepted rather than a false positive because the statement is true and worth one review; it is the kind of finding a reader is meant to confirm once and then suppress."
    },
    {
      "fingerprint": "fbf3831f58a6089b",
      "rule_id": "DJS-025",
      "verdict": "accepted_risk",
      "file": "base_requirements.txt",
      "line": 15,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-03",
      "note": "django-debug-toolbar in base_requirements.txt:15, pinned through to requirements.txt:4. NetBox handles this about as well as it can be handled with a single distribution: settings.py:472 lists debug_toolbar in INSTALLED_APPS and settings.py:511 removes it again under `if not DEBUG and 'collectstatic' not in sys.argv`, so the app is not installed on a normal production run. That is precisely why DJS-024 stays silent here and why this rule speaks instead: the package is still on the machine, and what keeps the toolbar off is the value of DEBUG rather than the absence of the code. NetBox is a self-hosted product whose operators are told to enable DEBUG when troubleshooting -- shipping the toolbar so that it works when they do is a deliberate product decision, not an oversight, and splitting the image in two to avoid it would cost their users more than it saves. Accepted rather than false positive because the statement is true and the exposure is real: an operator who leaves DEBUG on after an incident gets a SQL panel, and NetBox's own threat model would rather they knew that. The rule is low severity for exactly this reason -- it is a fact about the deployment, not an accusation."
    },
    {
      "fingerprint": "2a90d8d66199b623",
      "rule_id": "DJA-010",
      "verdict": "accepted_risk",
      "file": "netbox/extras/api/serializers_/events.py",
      "line": 44,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-04",
      "note": "True reading. Webhook.secret is the HMAC key NetBox signs outgoing payloads with, and the model's own help text says it is deliberately not transmitted in the request -- because it is transmitted here instead, in every GET of the webhook. The endpoint requires the extras.view_webhook permission, so this is an administrative disclosure rather than an open one, and NetBox intends it: an operator has to read the secret back to configure the receiving end. Accepted as designed. The rule is right that a shared signing key is served on every read, and a reviewer who does not already know that should be told."
    },
    {
      "fingerprint": "a962e1def2056c5b",
      "rule_id": "DJA-010",
      "verdict": "accepted_risk",
      "file": "netbox/users/api/serializers_/tokens.py",
      "line": 18,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-04",
      "note": "True reading, and deliberate. Token.key is the API token's stored value, marked read_only_fields = ('key',) -- which stops writes and guarantees reads, exactly the half that does not help. NetBox shows the key so an operator can retrieve a token they created; the endpoint is restricted to the token's owner or a user holding grant_token. Accepted as designed."
    },
    {
      "fingerprint": "e78d346346603dc8",
      "rule_id": "DJA-010",
      "verdict": "accepted_risk",
      "file": "netbox/users/api/serializers_/tokens.py",
      "line": 19,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-04",
      "note": "True reading. TokenSerializer declares token = CharField(default=Token.generate), so the full token value is both accepted and returned. This is how NetBox lets a caller supply their own token or read back a generated one, and it is the single most sensitive value the API handles. Accepted as designed, with the same ownership restriction as the key field on line 18."
    },
    {
      "fingerprint": "f905f00592dcacf5",
      "rule_id": "DJA-010",
      "verdict": "accepted_risk",
      "file": "netbox/users/api/serializers_/tokens.py",
      "line": 82,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-04",
      "note": "True reading. TokenProvisionSerializer exists to exchange a username and password for a token, so returning the token is the entire purpose of the endpoint -- there is no version of this view that does not do it. Accepted as designed. Related to the DJA-003 entry on the same view, which reports that it is reachable anonymously; both are true and both are intentional."
    },
    {
      "fingerprint": "68473210b515642c",
      "rule_id": "DJA-010",
      "verdict": "accepted_risk",
      "file": "netbox/users/api/serializers_/tokens.py",
      "line": 96,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-04",
      "note": "True reading, same endpoint as line 82. The provisioning response carries key alongside token so a caller can identify the credential it was just issued. Accepted as designed."
    },
    {
      "fingerprint": "7f693b9790d115a5",
      "rule_id": "DJA-010",
      "verdict": "accepted_risk",
      "file": "netbox/users/api/serializers_/users.py",
      "line": 35,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-04",
      "note": "True reading, low value. UserSerializer.is_active tells a caller which accounts can still log in, which is account-status disclosure rather than a credential leak -- hence MEDIUM. The endpoint requires users.view_user. Accepted: an administrative user API that could not report whether an account was active would be unable to do its job."
    },
    {
      "fingerprint": "1a112797fea25284",
      "rule_id": "DJA-010",
      "verdict": "accepted_risk",
      "file": "netbox/users/api/serializers_/users.py",
      "line": 36,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-04",
      "note": "True reading. Group membership on the user model is authority-granting information: it tells an attacker which account is worth phishing. Restricted to users.view_user, and managing group membership is what this endpoint is for. Accepted as designed."
    },
    {
      "fingerprint": "a1d4483d14114e3e",
      "rule_id": "DJA-010",
      "verdict": "accepted_risk",
      "file": "netbox/users/api/serializers_/users.py",
      "line": 43,
      "reviewer": "Zambagarrah",
      "reviewed": "2026-08-04",
      "note": "True reading, same argument as the groups field above. The effective permission list is the most direct answer to 'which account should I target', but a user-administration API cannot withhold it from the administrators using it. Accepted as designed."
    }
  ]
}
