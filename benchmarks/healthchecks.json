{
  "triage_version": 1,
  "fingerprint_version": "djaudit/v1",
  "target": "healthchecks",
  "repo": "https://github.com/healthchecks/healthchecks",
  "sha": "5086d282ea33831e8c86eb9d0802dc2602c66ef7",
  "max_false_positive_rate": 0.1,
  "findings": [
    {
      "fingerprint": "9f80d4e589623281",
      "rule_id": "DJS-003",
      "verdict": "accepted_risk",
      "file": "hc/settings.py",
      "line": 68,
      "note": "Real and deliberate. hc/settings.py:68 is `SECRET_KEY = envsecret(\"SECRET_KEY\", \"---\")`, so an instance deployed without the SECRET_KEY environment variable signs its sessions with three hyphens. That is not a key an attacker has to steal -- healthchecks publishes it, as `Default: ---`, in its own configuration documentation, so anyone can forge a session for any user on such an instance without ever seeing the source. Healthchecks also documents that the value 'should be set to a unique, unpredictable value', which makes this a knowing convenience for local development rather than an oversight. Firm rather than certain because a real deployment normally does supply the variable.",
      "reviewed": "2026-08-02",
      "reviewer": "Zambagarrah"
    },
    {
      "fingerprint": "204f74697636c950",
      "rule_id": "DJS-001",
      "verdict": "accepted_risk",
      "file": "hc/settings.py",
      "line": 70,
      "note": "Real and deliberate. hc/settings.py:70 is `DEBUG = envbool(\"DEBUG\", \"True\")`, so a deployment that does not set the DEBUG environment variable runs with DEBUG on. Healthchecks knows this: its own production checklist in README.md tells operators to 'make sure it is set to False'. Convenient for local development, a hazard for anyone who deploys without reading the checklist, which is exactly what this rule is for. Reported at tentative because the environment can override it.",
      "reviewed": "2026-08-02",
      "reviewer": "Zambagarrah"
    },
    {
      "fingerprint": "4eca41d92b1884e1",
      "rule_id": "DJS-009",
      "verdict": "true_positive",
      "file": "hc/settings.py",
      "line": 1,
      "note": "A genuine gap, not a configuration choice. Healthchecks never assigns SESSION_COOKIE_SECURE and, unlike DEBUG or SECRET_KEY, offers no environment variable for it either, so there is no supported way for an operator to turn it on short of patching settings.py. The project clearly expects a TLS-terminating proxy -- it reads SECURE_PROXY_SSL_HEADER from the environment at line 78 -- but a proxy does not set this flag, and without it a browser that touches the plain-HTTP URL even once transmits the session cookie in clear text on the wire. Firm rather than certain only because we cannot see the deployment; the absence of the setting itself is not in doubt.",
      "reviewed": "2026-08-02",
      "reviewer": "Zambagarrah"
    },
    {
      "fingerprint": "ab3fdeee85888849",
      "rule_id": "DJS-010",
      "verdict": "true_positive",
      "file": "hc/settings.py",
      "line": 1,
      "note": "Same gap as DJS-009 and same absence of any knob: CSRF_COOKIE_SECURE is never assigned and cannot be set from the environment. Medium rather than high because reading a CSRF token is a step towards forging a request rather than an immediate account takeover, but the fix is one line and there is no argument on the other side.",
      "reviewed": "2026-08-02",
      "reviewer": "Zambagarrah"
    },
    {
      "fingerprint": "1d24d2cef11f4856",
      "rule_id": "DJS-006",
      "verdict": "accepted_risk",
      "file": "hc/settings.py",
      "line": 1,
      "note": "Correct about the source and defensible in practice. Healthchecks reads SECURE_PROXY_SSL_HEADER from the environment, which says plainly that the intended deployment puts a TLS-terminating proxy in front, and such a proxy is the normal and better place to perform the HTTP-to-HTTPS redirect -- doing it in Django means the plain-HTTP request has already reached the application. This is precisely the case the rule reports at tentative rather than firm, because we cannot see the proxy from inside the repository. Unlike the two cookie flags above, delegating this one loses nothing.",
      "reviewed": "2026-08-02",
      "reviewer": "Zambagarrah"
    },
    {
      "fingerprint": "d29e1d665c2a9ae6",
      "rule_id": "DJS-007",
      "verdict": "accepted_risk",
      "file": "hc/settings.py",
      "line": 1,
      "note": "Correct, and quiet on purpose. Healthchecks mentions HSTS nowhere at all -- not in settings, not in its nginx or Docker configuration, not in its documentation. HSTS is the one security header whose absence from the Django settings is weak evidence: nginx, a load balancer and every CDN can send Strict-Transport-Security, it is extremely common to set it there, and none of that is visible from inside the repository. The rule reports this at low/tentative for exactly that reason, which is why it does not appear in a default run. Recorded as accepted rather than a false positive because the statement is true -- Django is not sending the header -- and because a project that assumes the proxy is doing it and is wrong has no way to find out except by being attacked.",
      "reviewed": "2026-08-02",
      "reviewer": "Zambagarrah"
    },
    {
      "fingerprint": "c2ad7b6cf884b40f",
      "rule_id": "DJS-020",
      "verdict": "true_positive",
      "file": "hc/settings.py",
      "line": 1,
      "note": "Real, and the rule's own caveat is the reason it needs a written answer. hc/settings.py never mentions AUTH_PASSWORD_VALIDATORS, so Django's validate_password() returns without checking anything, and that is a fact rather than an inference. Healthchecks is not left with no policy at all, though: hc/accounts/forms.py declares SetPasswordForm.password as forms.CharField(min_length=8), so the one form that sets a password does enforce a length. That is a partial mitigation and it is exactly the shape the rule warns it cannot see. It is still graded true_positive rather than accepted_risk for two reasons. First, min_length=8 alone accepts \"12345678\" and \"password\", which is precisely what credential stuffing tries first -- CommonPasswordValidator is the validator that matters here and there is no equivalent. Second, the check lives on one form, so it does not cover hc/accounts/management/commands/createsuperuser.py, which has to reimplement its own validate_password() to compensate; that duplication is itself the argument for moving the policy behind a validator, where every path that sets a password goes through it. Reported at medium/firm, which is the right size: this weakens passwords, it does not hand anyone an account."
    },
    {
      "fingerprint": "1c91c02f9536ee0e",
      "rule_id": "DJS-021",
      "verdict": "accepted_risk",
      "file": "hc/settings.py",
      "line": 229,
      "note": "Correctly identified and correctly graded low/tentative, which is the point of this entry. hc/settings.py:229 is `\"CONN_MAX_AGE\": envint(\"DB_CONN_MAX_AGE\", \"0\")` inside the Postgres branch, so an instance that does not set DB_CONN_MAX_AGE opens and closes a connection per request. That is real and it is what the rule says. It is accepted_risk rather than true_positive because Healthchecks is self-hosted software rather than one deployment: the setting is deliberately exposed as an environment variable and documented, so the project has handed the decision to the operator, which is the correct thing for it to do. The value of the finding is for the operator reading their own deployment, not for the project. The confidence lands at tentative on its own -- the value is env-dependent and the assignment sits inside an `if`, and the shared policy charges for both -- which keeps it below the default output threshold, exactly where a performance nudge with an invisible mitigation belongs."
    },
    {
      "fingerprint": "4348a03c883c5b4a",
      "rule_id": "DJS-022",
      "verdict": "accepted_risk",
      "file": "hc/settings.py",
      "line": 233,
      "note": "Real and precisely located. hc/settings.py:233 is `\"sslmode\": os.getenv(\"DB_SSLMODE\", \"prefer\")`, and docker/.env.example ships `DB_SSLMODE=prefer` as well, so the documented starting point for a Docker deployment is the mode that will accept a plaintext session without saying so. An instance whose database is on another host and whose operator never changed the variable sends the database password and every row in the clear, and nothing anywhere reports it, because libpq downgrades silently. accepted_risk rather than true_positive for the same reason as DJS-021: Healthchecks is self-hosted software, not a deployment, and it has exposed the setting as a documented environment variable, which is the correct thing for it to do -- many self-hosters run the database on the same host over a Unix socket, where sslmode genuinely does not matter, and forcing `require` there would break them. The finding is for the operator, not the project, and medium/tentative is the right size for that: worth reading, not worth failing a build over. Whether the connection is local is exactly what the rule cannot see here, since HOST is os.getenv(\"DB_HOST\", \"\") -- an empty default that means Unix socket, overridden at deployment time by a value we cannot read. That env-dependence is also what puts the confidence at tentative rather than firm, which is the honest answer."
    }
  ]
}
