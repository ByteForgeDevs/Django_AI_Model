# Changelog

Generated from the commit trail by `scripts/gen_changelog.py`; edit the commits,
not this file. `scripts/gen_changelog.py --check` keeps the two in step.

Versions follow [semantic versioning](https://semver.org), with the pre-1.0
caveat that the minor carries breakage below `1.0.0`. `docs/versioning.md`
explains what counts as a breaking change to the report schema and to
fingerprints, and therefore to any baseline you have committed.

## Unreleased (0.4.0)

### Upgrading

- **Report schema 1.** The JSON report shape or its enum vocabularies changed; see `docs/versioning.md`.
- **Report schema 2.** The JSON report shape or its enum vocabularies changed; see `docs/versioning.md`.
- **Report schema 3.** The JSON report shape or its enum vocabularies changed; see `docs/versioning.md`.
- **Report schema 4.** The JSON report shape or its enum vocabularies changed; see `docs/versioning.md`.
- **Fingerprints are now `djaudit/v1`.** Finding identity changed, so existing baseline files no longer match and must be regenerated with `djaudit run --write-baseline`, or every finding they held will be reported as new.

### Added

- **BREAKING** **config:** override rule severity before the threshold, not after (eace456f)
- **BREAKING** **config:** exclude paths by filtering findings, not by skipping files (0493c244)
- **config:** read [tool.djaudit], and ask click who set what (9acc37cf)
- **BREAKING** **container:** build the image from source, so a tag cannot publish other code (e3228744)
- **pre-commit:** audit the project, because auditing the diff finds nothing (715df392)
- **action:** upload the findings before failing the job on them (c75af97f)
- **changelog:** derive the release notes from the trail that already exists (0ad7e0c4)
- **schema:** make the published contract something you cannot change quietly (8204c650)
- **cli:** let a run ask for the external tools, and never by default (b80af89d)
- **adapters:** merge by fingerprint, and make a subsumed claim answerable (03701f95)
- **adapters:** audit pinned dependencies without installing any of them (81619ff6)
- **adapters:** decline the bandit adapter, and keep the reason checkable (f9b87c52)
- **adapters:** keep 41 of ruff's 13,388 findings (ea80e421)
- **adapters:** decide what an external tool's findings are worth (20a92152)
- **rules:** add DJX-009, column limits SQLite accepts and Postgres rejects (52885bff)
- **rules:** add DJX-008, a regex lookup that means different things per engine (4ec95e2d)
- **rules:** add DJX-007, select_for_update() takes no lock on SQLite (ce9a013b)
- **rules:** add DJX-006, a constraint SQLite does not enforce (0aa4f319)
- **rules:** add DJX-005, Postgres-only field types in a project that also runs SQLite (56d93496)
- **rules:** add DJX-004, case-sensitive text lookups in a project that also runs SQLite (fdc66130)
- **rules:** add DJX-002, JSONField containment in a project that also runs SQLite (28fe084f)
- **rules:** add DJX-003, distinct('field') in a project that also runs SQLite (3f22d4e7)
- **djx:** report development and production on different engines (4da84fc7)
- **djx:** decide whether a project can reach two databases (db18bb2c)
- **djx:** read every database a settings module can reach (44c74171)
- **rules:** add DJS-028, the rule that audits our own recall (65939c94)
- **live:** merge Django's deployment check with our own DJS findings (23436580)
- **live:** adapt manage.py check --deploy into our schema (0e620b4d)
- **live:** scale DJM-010 severity by measured table size (5d522808)
- **rules:** add DJM-010, the first live-tier rule (73e71e3b)
- **live:** read real emitted SQL and classify its Postgres locks (38ac5670)
- **cli:** --live/--no-live, off by default, disclosed before the target runs (be36dbc3)
- **engine:** report what the live tier did not check, and stop --rule bypassing consent (e608222c)
- **live:** ask the target's Django what it is, without dumping settings (aa405ecb)
- **live:** add the subprocess runner for the live tier (9767e6b9)
- **live:** detect the target's interpreter without executing it (84a0830e)
- **rules:** add DJM-009 AddConstraint validated under a lock (a9f5aba4)
- **rules:** add DJM-008 schema and data in one transaction (7fb6d102)
- **rules:** add DJM-007, unbounded queryset in a data migration (a7f38066)
- **rules:** add DJM-006, an irreversible data pass that revokes a reverse (e20edc17)
- **rules:** add DJM-005, renames that break rolling deploys (2fae8bbe)
- **rules:** add DJM-004, RemoveField breaking a rolling deploy (b09f3ec8)
- **rules:** add DJM-003, AddIndex without CONCURRENTLY (e5b29766)
- **rules:** add DJM-002, AlterField rewriting or scanning a table (21bf925f)
- **rules:** add DJM-001, and settle how the migration family reports (bdde727a)
- **migrations:** replay migration state so an AlterField can say what a column was (40165d37)
- **migrations:** resolve the graph the way Django's loader does, squashes and all (4124c5d0)
- **migrations:** read migration files as text, because running them is the risk (8c2217c7)
- **provenance:** label every statement with what authored it (f025e9f5)
- **llm:** verify patches against a scratch copy before offering them (665f61e9)
- **llm:** propose patches for the four findings whose fix the rule decided (d8c6d785)
- **llm:** edit by verified byte range, and measure libcst out of the build (dcddda35)
- **llm:** frame findings for the people who decide, not the people who fix (426a7009)
- **llm:** explain a finding from the finding, and never from the file (fc592e77)
- **llm:** group findings by the key that never merges a disagreement (ab33cf35)
- **llm:** propose suppressions only where a model actually looked (87fb7a45)
- **llm:** ask only where the answer was ever in doubt, and measure what "settled" costs (e8b56d24)
- **llm:** redact the prompt, not the two parts of it I happened to look at (36565972)
- **llm:** score the triage layer before building it, and find it has little room (687123a1)
- **llm:** running out of budget is the same as having no model (4ec8f308)
- **llm:** a cache key that covers every input, including the one I missed (09b7efda)
- **llm:** configuration that will not hold the key it needs (6b27769b)
- **llm:** a provider that cannot answer a question it was not asked (e4247b93)
- **rules:** add DJI-012 request data choosing a file path (80bf5325)
- **rules:** add DJI-011 request data marked as trusted HTML (8db9870e)
- **rules:** add DJI-010 request data choosing a redirect target (2e96e1a6)
- **rules:** add DJI-009 request data choosing an outbound host (09819001)
- **rules:** add DJI-008 request data reaching a shell (f238bf93)
- **rules:** add DJI-007 unsafe deserialisation of tainted data (d8b2c633)
- **rules:** add DJI-006 request-driven ordering with no allowlist (71005e9e)
- **rules:** add DJI-005 request data expanded into queryset keywords (72ddec54)
- **rules:** add DJI-004 request data interpolated into RawSQL or Func (916fa14e)
- **rules:** add DJI-003 request data interpolated into a .extra() clause (b7248075)
- **rules:** add DJI-002 request data interpolated into a .raw() query (f7022cc1)
- **rules:** add DJI-001 request data interpolated into a cursor statement (21c3ce3a)
- **rules:** add DJP-010 client-controllable ordering on an unindexed column (f52ac15a)
- **rules:** add DJP-009 deferred field reload, fix two cancelling dataflow bugs (9251893a)
- **rules:** add DJP-008, a whole table read into memory (f93fe95e)
- **rules:** add DJP-007, a write issued once per row of a loop (45756e8e)
- **rules:** add DJP-006, .count() used only to test whether rows exist (32fe7f4b)
- **rules:** add DJP-005, len(queryset) where the rows are discarded (370dc5ec)
- **dataflow:** read to_attr, GenericPrefetch and nested Prefetch querysets (d3a4065f)
- **rules:** count the round trips a loop makes, not the names that hold them (7897260c)
- **rules:** find the N+1 that has no loop to look for (9990278b)
- **rules:** report the fetch that needs a second query, not a wider join (185618bd)
- **rules:** report the join that was available and was not taken (59905d98)
- **dataflow:** follow a queryset one call hop, and measure that it does not pay (70f2ad8b)
- **dataflow:** say which variable is a row, and a row of what (f5ccf7b5)
- **dataflow:** read what a queryset already fetched, not just what it is (d4660a0c)
- **dataflow:** say which model a queryset holds, not just that it is one (b77807a9)
- **dataflow:** answer "which value does this name hold *here*" (3874252c)
- **dataflow:** answer "what is this name" without guessing (b844daeb)
- **rules:** report the page that cannot promise what is on it (747f2184)
- **rules:** report the column with two spellings of empty (035b4061)
- **rules:** report the invoice that leaves with the user (6873ae22)
- **registry:** register the data model design family (af320842)
- **rules:** report the login form that answers as fast as it is asked (637f7067)
- **rules:** report the query parameter nobody reviewed (6b762657)
- **rules:** report the pagination setting that paginates nothing (4f1691bb)
- **rules:** report the secret that is not in the field list you read (086498e5)
- **rules:** report the request body choosing whose record this is (720f3aee)
- **rules:** report the secret that comes back on every read (837e9e65)
- **rules:** report the field list that names what to hide (99217ae6)
- **rules:** report the field list that lets the model decide (1f7c5867)
- **rules:** report endpoints whose auth and permissions contradict (7a139383)
- **rules:** report the function view whose decorator nobody added (1b6eba15)
- **rules:** report object permissions that are written and never run (950165de)
- **rules:** report list endpoints that hand back everyone's rows (9eadfe7f)
- **rules:** report anonymous writes on a view that opened itself (d6a9322b)
- **rules:** report the view that never said who may call it (34e70214)
- **rules:** resolve who may reach an endpoint, and say which file decided (3525560e)
- **api:** resolve which rows an endpoint can reach (4206e9bc)
- **api:** resolve what actually guards each endpoint (639c1794)
- **api:** resolve routers and urlconfs into reachable endpoints (7a856314)
- **api:** discover DRF views, viewsets and function views (5923fce6)
- **api:** read what a serializer actually puts on the wire (c594d7a7)
- **graph:** walk the model graph to the user who owns a row (7840e739)
- **graph:** resolve managers and the querysets behind them (d06b5384)
- **graph:** resolve inheritance across module boundaries (f585f1e9)
- **graph:** read class Meta against Django's semantics (2d02387d)
- **graph:** name reverse relations the way Django does (a49c05ae)
- **graph:** follow relations, including the one that names the user (1de0f4c1)
- **graph:** read the fields, and say which ones we could not read (27fa4086)
- **graph:** find the models, without importing the project (a57647fc)
- **rules:** report error reporting configured to send more than it redacts (035dcc9f)
- **rules:** read the urlconf and report the admin at its default path (8b0118ac)
- **rules:** report development tooling shipped in the production dependency set (47d42a83)
- **rules:** detect development tooling left in production INSTALLED_APPS (c1e14403)
- **rules:** add DJS-023 for database keys Django never reads (85e9d3c7)
- **rules:** add DJS-022 for Postgres sessions that can run in the clear (21ab3f0f)
- **rules:** add DJS-021 for connections that are never reused (53b0a4e8)
- **rules:** add DJS-020 for a missing password policy (5ace5270)
- **rules:** add DJS-019 for a weak leading password hasher (ff9f00fc)
- **rules:** add DJS-018, MIME type sniffing is allowed (748a47f8)
- **rules:** add DJS-017, clickjacking protection is off (08a6ffd9)
- **rules:** add DJS-016, CORS wildcard paired with credentials (1e62df4e)
- **rules:** add DJS-015, CORS open to every origin (bdadf627)
- **rules:** add DJS-014, CSRF_TRUSTED_ORIGINS too broad or inert (45bdbdb7)
- **rules:** add DJS-013, ALLOWED_HOSTS is a wildcard or empty (a6748f81)
- **rules:** add DJS-012, SECURE_PROXY_SSL_HEADER trusts a request header (035b453e)
- **rules:** add DJS-008, HSTS does not cover subdomains (38c91927)
- **rules:** add DJS-007, HSTS not enforced for at least a year (cc167cfe)
- **rules:** add DJS-011, SESSION_COOKIE_HTTPONLY disabled (1301b6b4)
- **rules:** add DJS-010, CSRF_COOKIE_SECURE disabled (7fa36ddb)
- **rules:** add DJS-009, SESSION_COOKIE_SECURE disabled (453b24a2)
- **rules:** add DJS-006, SECURE_SSL_REDIRECT not enabled (5cf18d64)
- **djs-005:** report a service credential readable in the source (0e41e807)
- **djs-004:** report a database password readable in the source (5fc95520)
- **djs-003:** report a SECRET_KEY that can be guessed rather than stolen (ec38aa4d)
- **djs-002:** report a SECRET_KEY that is readable in the source (11c370a7)
- **settings:** grade findings from resolution quality with one shared policy (8e8746c8)
- **settings:** resolve INSTALLED_APPS and MIDDLEWARE as assembled, not declared (d4c1c242)
- **settings:** Django defaults table, cross-checked against Django itself (28406bc1)
- **settings:** resolve inheritance across star imports (f75f5df8)
- **settings:** ResolvedSetting with a full override chain (3fb1ee99)
- **evaluator:** resolve getattr indirection against a configuration object (c639fdf3)
- **evaluator:** follow local helpers, django-environ and python-decouple (1d5d808c)
- **evaluator:** conditionals, comparisons and comprehensions (a5957db2)
- **evaluator:** resolve environment variable reads to their defaults (50a2a763)
- **evaluator:** resolve literals, containers and string building (5109af5d)
- **values:** tri-state result type for partial evaluation (545193e5)
- **summary:** publish precision and recall to the CI job summary (4207e85a)
- **benchmark:** score a run against recorded verdicts and gate on it (34a39c0f)
- **triage:** record human verdicts for benchmark findings (df826e94)

### Fixed

- **docs:** gate the versions the docs tell people to install (aa150376)
- **changelog:** read an absent tag the same way twice (f07521a1)
- **adapters:** call a tool by its name, not by the path it lives at (0550e926)
- **scripts:** run the mutant the harness says it ran (59144fe8)
- **live:** use -E -s -B -u, because -I made manage.py unrunnable (ad5db6e6)
- **rules:** re-key two recursion guards on the node, not the name (289d21c5)
- **dataflow:** walk into keyword arguments in both AST walkers (f0cb2c5d)
- **graph:** record the indexes Django creates without being asked (c3177234)
- **eval:** stop scoring a run that never happened (5c3b96d0)
- **plan:** check the plan against the code, not against itself (2e14c579)
- **api:** read the serializers whose parent we cannot see (5d67cad4)
- **discovery:** refuse to pass silently on a project we cannot read (d3648825)
- **rules:** check that something reads the setting, not just its value (2bfac62a)
- **settings:** only prune a branch we can prove never runs (59371ffd)
- **evaluator:** support dict and sequence methods, not just string ones (a2670658)
- **sarif:** point $schema at a URL that resolves, and gate on conformance (48a44183)
- **cli:** create parent directories for terminal --output too (91198959)
- **sarif:** resolve associatedRule and build descriptors from RuleMeta (320db9e1)
- **suppression:** stop `# djaudit: ignore[]` suppressing every rule (9588bace)

### Performance

- **context:** cache def-use chains and queryset tracking per scope (ac16a94c)
- **engine:** stop paying for full collections that free nothing (98d9f5a1)
- **routes:** read each module once instead of three times (2396a6d1)

### Changed

- **rules:** extract the settings-rule loop and grading into a base class (2f348a08)
- **djs-001:** read DEBUG from the resolver and delete the bespoke scan (70b64a65)

### Documentation

- **rules:** index every rule, and verify the anchors actually land (8311a710)
- **guide:** write the adoption path, and gate every command in it (3761a5ed)
- **config:** derive the configuration reference's contents from the code (cde7ffe5)
- **plan:** record the release-loop defects and correct the phase 7 status (b26abb49)
- **plan:** build the configuration Phase 7 assumed it could document (8a95c618)
- **adapters:** write down what we take from other tools, and gate it (b149e7a9)
- **live:** write the live tier's security model, and gate it (01c66a26)
- **llm:** write the failure modes down, then gate them against the code (b4e0fe7d)
- **plan:** pull the LLM layer forward, and put its measurement first (eb2813f6)
- state the dataflow analysis limits, each one verified by running it (74866005)
- publish the N+1 redundant-report rate and correct a triage note (08eafef1)
- **plan:** stop the risk register describing protections that do not exist (ef7c3ce1)
- **rules:** give every family a page, and the graph a rationale (68380e9d)
- state what the tool actually measures, with the numbers (445bf4ef)
- **rules:** publish what each rule cannot see, generated from the code (ef16824e)
- **plan:** record Step 1.3 and the two amendments it forced (11bc4ffa)
- **plan:** record measured evaluator coverage for Step 1.2 (4bbf7944)
- **plan:** record the JSON decision and enforce the plan's own counts (ac0257e1)
- **plan:** add Step 1.0 for Phase 0 review follow-ups (be716358)
- **plan:** add full phase/step/substep project plan (0c66ddaf)

### Packaging

- **release:** publish from a tag through the gate, with one version source (26fd0968)
- check the DRF defaults table against DRF (93c13d10)
